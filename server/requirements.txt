# memory-mcp dependencies.
# Top-level only — transitive deps (openai, qdrant-client, pydantic, starlette,
# uvicorn) are pulled automatically.
#
# After first install: pip freeze > requirements.lock  (commit for reproducibility)
mem0ai
mcp
